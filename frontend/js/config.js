/**
 * Sofzenix HackFest — Frontend Configuration
 * 
 * Set API_URL to your backend's public address (e.g., Ngrok, Localtunnel).
 * Leave as empty string if backend and frontend are on the same domain.
 */
const CONFIG = {
  // If your backend is local, set this to http://127.0.0.1:5000:
  API_URL: window.location.origin, 
};
